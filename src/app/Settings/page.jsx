import Settings from "../../components/pages/Settings";

export const metadata = {
  title: "Settings",
  description: "Recorder Einstellungen für Upload und Metadaten.",
};

export default function Page() {
  return <Settings />;
}
